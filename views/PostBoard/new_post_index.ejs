<!DOCTYPE html>
<html>
  <head>
    <%- include ../partials/head.ejs %>
    <link rel="stylesheet" href="/stylesheets/text.css">
    <script>
      $(document).ready(function(){
        $('li.disabled>a').on('click',function(e){
          e.preventDefault();
        });
      });
    </script>
  </head>
  <body>
    <%- include ../partials/header+nav.ejs %>
    <div class="page-wrap">
        <section class="text">
            <div class="container">
              <div class="row">
                 <div class="col-xs-6">
                     <h2><%= main_menu %></h2>
                 </div>
                 <div class="col-xs-5 offset-xs-1 circlepic">
                    <div class="circle" id="circle1"></div>
                    <div class="circle" id="circle2"></div>
                    <div class="circle" id="circle3"></div>
                </div>
             </div>
             <div class="row">
                <div class="col-xs-1"></div>
                <div class="col-xs-8">
                 <div class="col-xs-8"></div>
                     <table class="mainText">
                       <tr>
                           <th class="number">번호</th>
                           <th class="title">[경로]제목</th>
                           <th class="author">작성자</th>
                           <th class="date">작성일</th>
                           <th class="views">조회</th>
                           
                       </tr>
                       <% var post_num=count; %>
                       <% for(var i=0;i<data.length;i++){ %>
                       <tr>
                           <th class="number"><%= post_num %></th>
                           <th class="title"><a href="/<%=data[i].path%>/<%= data[i].post._id %>" ><span style="color:#bbb">[<%=data[i].title %>]</span><%= data[i].post.title %></a><span style="color:skyblue">(<%= data[i].post.comments.length %>)</span></th>
                           <th class="author"><%= data[i].post.author ? data[i].post.author.name : "" %></th>
                           <th class="date"><small><%= data[i].post.createdAt.toLocaleDateString() %></small></th>
                           <th class="views"><%= data[i].post.views %></th>
                       </tr>
                       <% post_num-- %>
                       <% } %>
                   </table>
<!--이곳에 자기가 원하는 레이아웃을 설정해 주도록 하자.-->
                  </div>
                </div>
<%- include ../partials/footer.ejs %>